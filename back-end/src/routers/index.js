const userRouters = require('./user.routers');
const productRouters = require('./product.routers');
const registerRouters = require('./register.router');
const orderRouters = require('./order.router');
const saleRouters = require('./sale.router');

module.exports = {
  userRouters,
  productRouters,
  registerRouters,
  orderRouters,
  saleRouters,
};
